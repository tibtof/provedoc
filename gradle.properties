# Coordinates. A future release workflow overrides `version` from the git tag
# (v1.2.3 -> -Pversion=1.2.3); this value is what local/CI snapshot builds use.
group=dev.provedoc
version=0.1.0-SNAPSHOT
